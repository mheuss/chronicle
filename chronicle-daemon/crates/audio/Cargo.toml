[package]
name = "chronicle-audio"
version = "0.1.0"
edition = "2024"
description = "Audio capture — microphone via AVFoundation, system audio via ScreenCaptureKit"
license = "MIT"

[dependencies]
objc2 = "0.6"
objc2-av-foundation = "0.3"
objc2-screen-capture-kit = "0.3"
